import React from "react";

export default function Projects() {
  return (
    <main className="max-w-4xl mx-auto px-4 pt-8">
      <section id="projects" className="py-8">
        <h1 className="text-3xl font-bold text-[var(--fg)]">Projects</h1>
        <p className="mt-3 text-[var(--muted)]">Project list / cards will appear here.</p>
      </section>
    </main>
  );
}